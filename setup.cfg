[flake8]
max-line-length = 120
exclude =
    .git,
    __pycache__,
    build,
    dist,
    venv,
    .venv
ignore = 
    E203,
    W503

[tool:pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short